traj_name: Demo1k
obs_mode: "rgbd"
control_mode: "pd_ee_delta_pose"
num_traj: 1000
only_count_success: true
num_procs: 20
clip: true
seed: 0

motion_planning_args:
  env_id: pass
  env_kwargs:
    obs_mode: null
    control_mode: pd_joint_pos
    render_mode: "rgb_array"
    sim_backend: "auto"
    sensor_configs:
      shader_pack: default
    human_render_camera_configs:
      shader_pack: default
    viewer_camera_configs:
      shader_pack: default
  num_traj: ${..num_traj}
  only_count_success: ${..only_count_success}
  reward_mode: null
  vis: false
  save_video: false
  traj_name: ${..traj_name}
  num_procs: ${..num_procs}
  solve_kwargs:
    pre_insert_aug: 0
    start_after_grasp: false
  seed: ${..seed}
